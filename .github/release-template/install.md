## Install

Install through WKOpenVR's Face Tracking module source. In WKOpenVR, open the Face Tracking module source list, enable prerelease entries when using a beta build, then install **WKOpenVR Synthetic Face Module**.

Manual path: download the module zip and matching manifest from this release, then add them through a registry source or local native-module install flow.
